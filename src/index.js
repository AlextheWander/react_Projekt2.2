import ReactDOM from 'react-dom'
import App from './App' // fuctionel component importet fra jsx 
import './index.css'

ReactDOM.render(<App />, document.querySelector('#root')) //Render til root id i HTML og med npm start i terminalen som starter serveren/appen i browseren